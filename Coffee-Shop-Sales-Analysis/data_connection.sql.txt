-- SQL used for data access and validation
-- Data was imported from Excel into the database
-- Analysis performed using Python and Power BI

-- Verify imported data
SELECT *
FROM coffee_shop;

-- Basic row count check
SELECT COUNT(*) 
FROM coffee_shop;
